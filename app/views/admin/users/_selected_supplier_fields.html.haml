- current_page_ids = @suppliers.map { |s| s.salesforce_id.to_s }
- selected_ids = Array(selected_ids).map(&:to_s)
- preserved_ids = selected_ids - current_page_ids

- preserved_ids.each do |id|
  = hidden_field_tag "supplier_salesforce_ids[]", id